{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["npm install"]
    },
    "build": {
      "commands": ["true"]
    }
  },
  "deploy": {
    "startCommand": "npm run worker:drive"
  }
}
